<a href="{{id}}" class="list-group-item list-group-item-action">
  <div class="d-flex justify-content-between">
    <div>
      <h3>{{room_name}}</h3>
 
      <h5>{{user.username}}</h5>
    </div>
    <div>
      <span class="badge badge-info pull-right p-2">
      <i class="far fa-user pull-right" id="roomicon"></i><span class="ml-2">{{getRoomUsers id}}</span></i>
      </span>
    </div>
  </div>
</a>